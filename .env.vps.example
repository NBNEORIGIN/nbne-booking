# NBNE Booking - VPS Production Environment Variables
# Copy this file to .env and fill in the values
# Location: /srv/booking/.env

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Connect to existing nbne-postgres container
DATABASE_URL=postgresql://nbne_admin:YOUR_POSTGRES_PASSWORD@nbne-postgres:5432/nbne_main

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# CORS origins (comma-separated)
BACKEND_CORS_ORIGINS=https://booking-beta.nbnesigns.co.uk

# ============================================
# EMAIL CONFIGURATION (Optional but recommended)
# ============================================
# SMTP settings for transactional emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
FROM_EMAIL=noreply@nbnesigns.co.uk
FROM_NAME=NBNE Booking

# Feature flags
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_SMS_NOTIFICATIONS=false

# ============================================
# NOTES
# ============================================
# 1. Replace YOUR_POSTGRES_PASSWORD with actual password from nbne-postgres
# 2. Get Gmail app password from: https://myaccount.google.com/apppasswords
# 3. Ensure nbne-postgres container is on nbne-network
# 4. Never commit this file with real secrets
